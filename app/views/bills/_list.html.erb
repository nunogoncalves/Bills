<ul class="bills_list">
	<% @dashboard_presenter.possible_bills.each do |bill| %>
		<%= render 'bills/bill', bill: bill %>
	<% end %>
</ul>
ul><script>
	$(function() {
		$.bills.bills_array = <%= raw @dashboard_presenter.possible_bills_names %>;
		$.bills.loadAllPickers();		
	})
</script>