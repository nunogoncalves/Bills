<ul class="bills_main_info">
	<% @dashboard_presenter.possible_bills.each do |bill| %>
		<%= render 'bill', bill: bill %>
	<% end %>
</ul>

<script>
	$(function() {
		$.bills.bills_array = <%= raw @dashboard_presenter.possible_bills %>;
		$.bills.loadAllPickers();
	});
</script>